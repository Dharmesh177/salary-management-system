-- Domain tables (employees, salary_events, users) will be added incrementally
-- through TDD. This file exists so the migration runner has a checked-in
-- starting point and schema evolution is already in place.

SELECT 1;
